#include "menu.h"
#include "rect.h"
#include <algorithm>

void new_function(renderer rend)
{
	// float min_sz = std::min(rend.width, rend.height);
	// rect menu_rect = rect
	// (
	// 	(rend.width - min_sz) / 2,
	// 	(rend.height - min_sz) / 2,
	// 	min_sz / 2,
	// 	min_sz / 2
	// );
	// DrawRectangleRounded(menu_rect, 1, 20, RED);
	// DrawRectangleRec(menu_rect, RED);
}

void draw_menu(renderer rend)
{
	// float min_sz = std::min(rend.width, rend.height);
	// rect menu_rect = rect
	// (
	// 	(rend.width - min_sz) / 2,
	// 	(rend.height - min_sz) / 2,
	// 	min_sz / 2,
	// 	min_sz / 2
	// );
	// DrawRectangleRounded(menu_rect, 1, 20, RED);
	// DrawRectangleRec(menu_rect, RED);
}
